# Vercel KV Environment Variables
# These should be set in your Vercel project settings

# If you set up Vercel KV, it creates these automatically:
KV_URL=your_redis_url_here
KV_REST_API_URL=your_rest_api_url_here
KV_REST_API_TOKEN=your_token_here

# If you're using a standard Redis URL instead:
REDIS_URL=redis://default:password@host:port

# Note: The app will use whichever is available
# Vercel KV (recommended) uses KV_URL
# Standard Redis uses REDIS_URL
